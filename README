Source Code to Image

Installation

go get github.com/nayzawoo/ss

Usage

NAME:
   Code Screenshot - ss [filepath]

USAGE:
   ss [global options] command [command options] [arguments...]

VERSION:
   0.0.0

COMMANDS:
     help, h  Shows a list of commands or help for one command

GLOBAL OPTIONS:
   --lang value, -l value    Language (default: "auto")
   --output value, -o value  Output File (default: "home")
   --style value, -s value   Theme Style (default: "monokai")
   --help, -h                show help
   --version, -v             print the version